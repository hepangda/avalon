# Production defaults for docker-compose.prod.yml.
# scripts/prod-up.sh creates .env.prod from this file on first run.

APP_PORT=8001
POSTGRES_USER=avalon
POSTGRES_PASSWORD=change-this-password
POSTGRES_DB=avalon